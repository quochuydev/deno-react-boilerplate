// Settings screen. Boilerplate placeholder — wire kiosk configuration,
// device pairing, or admin controls here.
export function Settings() {
  return (
    <section className="card">
      <h1>Settings</h1>
      <p className="muted">
        Boilerplate placeholder — add kiosk configuration here.
      </p>
    </section>
  );
}
